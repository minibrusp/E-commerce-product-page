import styled from 'styled-components';

export const StyledCartButtonIcon = styled.div`
  position: relative;
`;

export const StyledCounter = styled.span`
  position: absolute;
  top: -7px;
  right: -5px;
  background-color: var(--primary-orange);
  color: var(--neutral-white);
  font-size: 10px;
  padding: 1px 6px;
  border-radius: 30px;
`;
